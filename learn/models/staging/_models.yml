version: 2
models:
  - name: stg_customers
    description: "Cleaned customer records"
    columns:
      - name: customer_id
        description: 'Unique identifier for each customer'
        data_tests:
          - unique
          - not_null
      
      - name: email
        data_tests:
          - not_null

